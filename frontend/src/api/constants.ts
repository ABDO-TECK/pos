/**
 * ثوابت API مركزية — تُستخدم في جميع استدعاءات الـ endpoints.
 * عدّل هذه القيم مرة واحدة لتنعكس في كل المشروع.
 */

/** الحد الافتراضي لعدد العناصر في كل صفحة */
export const DEFAULT_PAGE_SIZE = 50

/** الصفحة الأولى */
export const DEFAULT_PAGE = 1
